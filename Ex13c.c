#include <stdio.h>

int main(int argc, char const *argv[])
{
    // int numbers[5] = {10, 20, 30, 40, 50};

    // printf("The first element of the array is: %d\n", numbers[0]);
    // printf("The third element of the array is: %d\n", numbers[2]);

    int loop[7] = {5, 6, 5, 3, 1, 3, 5};

    printf("The first element of the array is: %d\n", loop[0]);
    printf("The first element of the array is: %d\n", loop[1]);
    printf("The first element of the array is: %d\n", loop[2]);
    printf("The first element of the array is: %d\n", loop[3]);
    printf("The first element of the array is: %d\n", loop[4]);
    printf("The first element of the array is: %d\n", loop[5]);
    printf("The first element of the array is: %d\n", loop[6]);

    return 0;
}
