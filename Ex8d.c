#include <stdio.h>
#include <conio.h>

int main(int argc, char const *argv[])
{
    int radius;
    float perimeter;
    radius = 6;

    perimeter = 2 * 3.14 * radius;
    printf("The perimeter of the circle is = %f inches \n", perimeter);

    return 0;
}
