#include <stdio.h>

int main(int argc, char const *argv[])
{
    // int numbers[5] = {10, 20, 30, 40, 50};

    // for (int i = 0; i < 5; i++)
    // {
    //     printf("numbers[%d] = %d\n", i, numbers[i]);
    // }

    int files[8] = {10, 20, 30, 14, 40, 50, 60, 34};

    for (int l = 0; l < 8; l++)
    {
        printf("files[%d] = %d\n", l, files[l]);
    }

    return 0;
}
