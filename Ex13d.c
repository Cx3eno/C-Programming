#include <stdio.h>

int main(int argc, char const *argv[])
{
    // int num[6] = {10, 20, 30, 40, 50, 60};

    // printf("The third element of the array is %d\n", num[2]);

    // num[2] = 494;
    // printf("The third element of the array now is %d\n", num[2]);

    int cold[5] = {23, 54, 32, 14, 67};

    printf("The fourth element of the array is %d\n", cold[3]);

    cold[3] = 78;
    printf("The fourth element of the array now is %d\n", cold[3]);

    return 0;
}
